---
{{ frontmatter | toyaml }}
---

{{ api.description }}

## Overview

{% if api_version %}- **Version**: {{ api_version }}
{% endif %}- **Base URL**: `{{ api.base_url }}`
- **Category**: {{ api.category | title }}
- **Tools Available**: {{ tool_count }}
- **Rate Limit**: {{ frontmatter.params.rate_limit or 'See documentation' }}
- **Documentation**: [{{ api.documentation_url or '#' }}]({{ api.documentation_url or '#' }})

## Quick Start

```python
from ocp_agent import OCPAgent

agent = OCPAgent()

# Register with authentication
await agent.register_api(
    '{{ api.name }}',
    headers={'Authorization': 'Bearer your_token_here'}
)

# List available tools
tools = agent.list_tools('{{ api.name }}')
print(f"Available: {len(tools)} tools")
```

## Authentication

**Type**: {{ api.authentication.type }}

{{ api.authentication.documentation }}

See the [Authentication Guide](/registry/authentication/) for general setup instructions.

## Available Tools

This API provides **{{ tool_count }} tools** across various resource types.

{% for group in tools | groupby('resource') %}
### {{ group[0] | resource_name }}

<div class="ocp-badge-container">
{% for tool in group[1] | sort(attribute='name') %}
<a href="tools/{{ tool.name | lower | replace('_', '-') | replace(' ', '-') }}" class="ocp-badge">
  <span class="ocp-badge-method {{ tool.method | lower | default('get') }}">{{ tool.method | upper | default('GET') }}</span>
  <span class="ocp-badge-name">{{ tool.name }}</span>
</a>
{% endfor %}
</div>

{% endfor %}

## Usage Example

```python
from ocp_agent import OCPAgent

# Initialize agent
agent = OCPAgent()

# Register the {{ api.display_name }}
api = await agent.register_api('{{ api.name }}')

# List available tools
tools = agent.list_tools('{{ api.name }}')
print(f"Available tools: {len(tools)}")

# Use a tool (example)
# result = await agent.call_tool('tool_name', parameters)
```

## Links

- [API Documentation]({{ api.documentation_url or '#' }})
- [OpenAPI Specification]({{ frontmatter.params.openapi_url }})
{% if frontmatter.params.contact.url %}- [Official Website]({{ frontmatter.params.contact.url }})
{% endif %}{% if frontmatter.params.contact.support_url %}- [Support]({{ frontmatter.params.contact.support_url }})
{% endif %}
